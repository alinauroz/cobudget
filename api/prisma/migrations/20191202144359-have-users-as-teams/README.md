# Migration `20191202144359-have-users-as-teams`

This migration has been generated by Gustav Larsson at 12/2/2019, 2:43:59 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."User" ADD COLUMN "dream" text   REFERENCES "public"."Dream"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Membership" DROP COLUMN "dream";

ALTER TABLE "public"."Dream" DROP COLUMN "description",
ADD COLUMN "description" text   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191202142418-change-name-of-members..20191202144359-have-users-as-teams
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("POSTGRES_URL")
 }
 model User {
   id          String       @default(cuid()) @id
@@ -38,17 +38,17 @@
   updatedAt   DateTime     @updatedAt
 }
 model Dream {
-  id                String       @default(cuid()) @id
-  slug              String       @unique
+  id                String   @default(cuid()) @id
+  slug              String   @unique
   title             String
-  description       String
+  description       String?
   event             Event
   images            String[]
-  team              Membership[]
+  team              User[]
   minFunding        Int?
   maxFunding        Int?
   budgetDescription String?
-  createdAt         DateTime     @default(now())
-  updatedAt         DateTime     @updatedAt
+  createdAt         DateTime @default(now())
+  updatedAt         DateTime @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191202144359-have-users-as-teams)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191202144359-have-users-as-teams'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
